// Code generated by SQLBoiler 3.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/null"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// BadPersonsAddress is an object representing the database table.
type BadPersonsAddress struct {
	ID          string      `boil:"id" json:"id" toml:"id" yaml:"id"`
	UpdatedAt   time.Time   `boil:"updated_at" json:"updatedAt" toml:"updatedAt" yaml:"updatedAt"`
	CreatedAt   time.Time   `boil:"created_at" json:"createdAt" toml:"createdAt" yaml:"createdAt"`
	BadPersonID null.String `boil:"bad_person_id" json:"badPersonID,omitempty" toml:"badPersonID" yaml:"badPersonID,omitempty"`
	CountryCode string      `boil:"country_code" json:"countryCode" toml:"countryCode" yaml:"countryCode"`
	Street      null.String `boil:"street" json:"street,omitempty" toml:"street" yaml:"street,omitempty"`
	City        null.String `boil:"city" json:"city,omitempty" toml:"city" yaml:"city,omitempty"`
	ZipCode     null.String `boil:"zip_code" json:"zipCode,omitempty" toml:"zipCode" yaml:"zipCode,omitempty"`
	Region      null.String `boil:"region" json:"region,omitempty" toml:"region" yaml:"region,omitempty"`
	Place       null.String `boil:"place" json:"place,omitempty" toml:"place" yaml:"place,omitempty"`
	PoBox       null.String `boil:"po_box" json:"poBox,omitempty" toml:"poBox" yaml:"poBox,omitempty"`
	Type        string      `boil:"type" json:"type" toml:"type" yaml:"type"`

	R *badPersonsAddressR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L badPersonsAddressL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var BadPersonsAddressColumns = struct {
	ID          string
	UpdatedAt   string
	CreatedAt   string
	BadPersonID string
	CountryCode string
	Street      string
	City        string
	ZipCode     string
	Region      string
	Place       string
	PoBox       string
	Type        string
}{
	ID:          "id",
	UpdatedAt:   "updated_at",
	CreatedAt:   "created_at",
	BadPersonID: "bad_person_id",
	CountryCode: "country_code",
	Street:      "street",
	City:        "city",
	ZipCode:     "zip_code",
	Region:      "region",
	Place:       "place",
	PoBox:       "po_box",
	Type:        "type",
}

// Generated where

var BadPersonsAddressWhere = struct {
	ID          whereHelperstring
	UpdatedAt   whereHelpertime_Time
	CreatedAt   whereHelpertime_Time
	BadPersonID whereHelpernull_String
	CountryCode whereHelperstring
	Street      whereHelpernull_String
	City        whereHelpernull_String
	ZipCode     whereHelpernull_String
	Region      whereHelpernull_String
	Place       whereHelpernull_String
	PoBox       whereHelpernull_String
	Type        whereHelperstring
}{
	ID:          whereHelperstring{field: "\"bad_persons_addresses\".\"id\""},
	UpdatedAt:   whereHelpertime_Time{field: "\"bad_persons_addresses\".\"updated_at\""},
	CreatedAt:   whereHelpertime_Time{field: "\"bad_persons_addresses\".\"created_at\""},
	BadPersonID: whereHelpernull_String{field: "\"bad_persons_addresses\".\"bad_person_id\""},
	CountryCode: whereHelperstring{field: "\"bad_persons_addresses\".\"country_code\""},
	Street:      whereHelpernull_String{field: "\"bad_persons_addresses\".\"street\""},
	City:        whereHelpernull_String{field: "\"bad_persons_addresses\".\"city\""},
	ZipCode:     whereHelpernull_String{field: "\"bad_persons_addresses\".\"zip_code\""},
	Region:      whereHelpernull_String{field: "\"bad_persons_addresses\".\"region\""},
	Place:       whereHelpernull_String{field: "\"bad_persons_addresses\".\"place\""},
	PoBox:       whereHelpernull_String{field: "\"bad_persons_addresses\".\"po_box\""},
	Type:        whereHelperstring{field: "\"bad_persons_addresses\".\"type\""},
}

// BadPersonsAddressRels is where relationship names are stored.
var BadPersonsAddressRels = struct {
	BadPerson string
}{
	BadPerson: "BadPerson",
}

// badPersonsAddressR is where relationships are stored.
type badPersonsAddressR struct {
	BadPerson *BadPerson
}

// NewStruct creates a new relationship struct
func (*badPersonsAddressR) NewStruct() *badPersonsAddressR {
	return &badPersonsAddressR{}
}

// badPersonsAddressL is where Load methods for each relationship are stored.
type badPersonsAddressL struct{}

var (
	badPersonsAddressAllColumns            = []string{"id", "updated_at", "created_at", "bad_person_id", "country_code", "street", "city", "zip_code", "region", "place", "po_box", "type"}
	badPersonsAddressColumnsWithoutDefault = []string{"bad_person_id", "street", "city", "zip_code", "region", "place", "po_box", "type"}
	badPersonsAddressColumnsWithDefault    = []string{"id", "updated_at", "created_at", "country_code"}
	badPersonsAddressPrimaryKeyColumns     = []string{"id"}
)

type (
	// BadPersonsAddressSlice is an alias for a slice of pointers to BadPersonsAddress.
	// This should generally be used opposed to []BadPersonsAddress.
	BadPersonsAddressSlice []*BadPersonsAddress

	badPersonsAddressQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	badPersonsAddressType                 = reflect.TypeOf(&BadPersonsAddress{})
	badPersonsAddressMapping              = queries.MakeStructMapping(badPersonsAddressType)
	badPersonsAddressPrimaryKeyMapping, _ = queries.BindMapping(badPersonsAddressType, badPersonsAddressMapping, badPersonsAddressPrimaryKeyColumns)
	badPersonsAddressInsertCacheMut       sync.RWMutex
	badPersonsAddressInsertCache          = make(map[string]insertCache)
	badPersonsAddressUpdateCacheMut       sync.RWMutex
	badPersonsAddressUpdateCache          = make(map[string]updateCache)
	badPersonsAddressUpsertCacheMut       sync.RWMutex
	badPersonsAddressUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single badPersonsAddress record from the query.
func (q badPersonsAddressQuery) One(ctx context.Context, exec boil.ContextExecutor) (*BadPersonsAddress, error) {
	o := &BadPersonsAddress{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for bad_persons_addresses")
	}

	return o, nil
}

// All returns all BadPersonsAddress records from the query.
func (q badPersonsAddressQuery) All(ctx context.Context, exec boil.ContextExecutor) (BadPersonsAddressSlice, error) {
	var o []*BadPersonsAddress

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to BadPersonsAddress slice")
	}

	return o, nil
}

// Count returns the count of all BadPersonsAddress records in the query.
func (q badPersonsAddressQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count bad_persons_addresses rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q badPersonsAddressQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if bad_persons_addresses exists")
	}

	return count > 0, nil
}

// BadPerson pointed to by the foreign key.
func (o *BadPersonsAddress) BadPerson(mods ...qm.QueryMod) badPersonQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.BadPersonID),
	}

	queryMods = append(queryMods, mods...)

	query := BadPersons(queryMods...)
	queries.SetFrom(query.Query, "\"bad_persons\"")

	return query
}

// LoadBadPerson allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (badPersonsAddressL) LoadBadPerson(ctx context.Context, e boil.ContextExecutor, singular bool, maybeBadPersonsAddress interface{}, mods queries.Applicator) error {
	var slice []*BadPersonsAddress
	var object *BadPersonsAddress

	if singular {
		object = maybeBadPersonsAddress.(*BadPersonsAddress)
	} else {
		slice = *maybeBadPersonsAddress.(*[]*BadPersonsAddress)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &badPersonsAddressR{}
		}
		if !queries.IsNil(object.BadPersonID) {
			args = append(args, object.BadPersonID)
		}

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &badPersonsAddressR{}
			}

			for _, a := range args {
				if queries.Equal(a, obj.BadPersonID) {
					continue Outer
				}
			}

			if !queries.IsNil(obj.BadPersonID) {
				args = append(args, obj.BadPersonID)
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(qm.From(`bad_persons`), qm.WhereIn(`bad_persons.id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load BadPerson")
	}

	var resultSlice []*BadPerson
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice BadPerson")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for bad_persons")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for bad_persons")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.BadPerson = foreign
		if foreign.R == nil {
			foreign.R = &badPersonR{}
		}
		foreign.R.BadPersonsAddresses = append(foreign.R.BadPersonsAddresses, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.BadPersonID, foreign.ID) {
				local.R.BadPerson = foreign
				if foreign.R == nil {
					foreign.R = &badPersonR{}
				}
				foreign.R.BadPersonsAddresses = append(foreign.R.BadPersonsAddresses, local)
				break
			}
		}
	}

	return nil
}

// SetBadPerson of the badPersonsAddress to the related item.
// Sets o.R.BadPerson to related.
// Adds o to related.R.BadPersonsAddresses.
func (o *BadPersonsAddress) SetBadPerson(ctx context.Context, exec boil.ContextExecutor, insert bool, related *BadPerson) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"bad_persons_addresses\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"bad_person_id"}),
		strmangle.WhereClause("\"", "\"", 2, badPersonsAddressPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.BadPersonID, related.ID)
	if o.R == nil {
		o.R = &badPersonsAddressR{
			BadPerson: related,
		}
	} else {
		o.R.BadPerson = related
	}

	if related.R == nil {
		related.R = &badPersonR{
			BadPersonsAddresses: BadPersonsAddressSlice{o},
		}
	} else {
		related.R.BadPersonsAddresses = append(related.R.BadPersonsAddresses, o)
	}

	return nil
}

// RemoveBadPerson relationship.
// Sets o.R.BadPerson to nil.
// Removes o from all passed in related items' relationships struct (Optional).
func (o *BadPersonsAddress) RemoveBadPerson(ctx context.Context, exec boil.ContextExecutor, related *BadPerson) error {
	var err error

	queries.SetScanner(&o.BadPersonID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("bad_person_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.R.BadPerson = nil
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.BadPersonsAddresses {
		if queries.Equal(o.BadPersonID, ri.BadPersonID) {
			continue
		}

		ln := len(related.R.BadPersonsAddresses)
		if ln > 1 && i < ln-1 {
			related.R.BadPersonsAddresses[i] = related.R.BadPersonsAddresses[ln-1]
		}
		related.R.BadPersonsAddresses = related.R.BadPersonsAddresses[:ln-1]
		break
	}
	return nil
}

// BadPersonsAddresses retrieves all the records using an executor.
func BadPersonsAddresses(mods ...qm.QueryMod) badPersonsAddressQuery {
	mods = append(mods, qm.From("\"bad_persons_addresses\""))
	return badPersonsAddressQuery{NewQuery(mods...)}
}

// FindBadPersonsAddress retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindBadPersonsAddress(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*BadPersonsAddress, error) {
	badPersonsAddressObj := &BadPersonsAddress{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"bad_persons_addresses\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, badPersonsAddressObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from bad_persons_addresses")
	}

	return badPersonsAddressObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *BadPersonsAddress) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no bad_persons_addresses provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	nzDefaults := queries.NonZeroDefaultSet(badPersonsAddressColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	badPersonsAddressInsertCacheMut.RLock()
	cache, cached := badPersonsAddressInsertCache[key]
	badPersonsAddressInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			badPersonsAddressAllColumns,
			badPersonsAddressColumnsWithDefault,
			badPersonsAddressColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(badPersonsAddressType, badPersonsAddressMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(badPersonsAddressType, badPersonsAddressMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"bad_persons_addresses\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"bad_persons_addresses\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into bad_persons_addresses")
	}

	if !cached {
		badPersonsAddressInsertCacheMut.Lock()
		badPersonsAddressInsertCache[key] = cache
		badPersonsAddressInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the BadPersonsAddress.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *BadPersonsAddress) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	key := makeCacheKey(columns, nil)
	badPersonsAddressUpdateCacheMut.RLock()
	cache, cached := badPersonsAddressUpdateCache[key]
	badPersonsAddressUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			badPersonsAddressAllColumns,
			badPersonsAddressPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update bad_persons_addresses, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"bad_persons_addresses\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, badPersonsAddressPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(badPersonsAddressType, badPersonsAddressMapping, append(wl, badPersonsAddressPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update bad_persons_addresses row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for bad_persons_addresses")
	}

	if !cached {
		badPersonsAddressUpdateCacheMut.Lock()
		badPersonsAddressUpdateCache[key] = cache
		badPersonsAddressUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q badPersonsAddressQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for bad_persons_addresses")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for bad_persons_addresses")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o BadPersonsAddressSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), badPersonsAddressPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"bad_persons_addresses\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, badPersonsAddressPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in badPersonsAddress slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all badPersonsAddress")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *BadPersonsAddress) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no bad_persons_addresses provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	nzDefaults := queries.NonZeroDefaultSet(badPersonsAddressColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	badPersonsAddressUpsertCacheMut.RLock()
	cache, cached := badPersonsAddressUpsertCache[key]
	badPersonsAddressUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			badPersonsAddressAllColumns,
			badPersonsAddressColumnsWithDefault,
			badPersonsAddressColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			badPersonsAddressAllColumns,
			badPersonsAddressPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert bad_persons_addresses, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(badPersonsAddressPrimaryKeyColumns))
			copy(conflict, badPersonsAddressPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"bad_persons_addresses\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(badPersonsAddressType, badPersonsAddressMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(badPersonsAddressType, badPersonsAddressMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert bad_persons_addresses")
	}

	if !cached {
		badPersonsAddressUpsertCacheMut.Lock()
		badPersonsAddressUpsertCache[key] = cache
		badPersonsAddressUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single BadPersonsAddress record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *BadPersonsAddress) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no BadPersonsAddress provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), badPersonsAddressPrimaryKeyMapping)
	sql := "DELETE FROM \"bad_persons_addresses\" WHERE \"id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from bad_persons_addresses")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for bad_persons_addresses")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q badPersonsAddressQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no badPersonsAddressQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from bad_persons_addresses")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for bad_persons_addresses")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o BadPersonsAddressSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), badPersonsAddressPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"bad_persons_addresses\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, badPersonsAddressPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from badPersonsAddress slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for bad_persons_addresses")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *BadPersonsAddress) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindBadPersonsAddress(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *BadPersonsAddressSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := BadPersonsAddressSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), badPersonsAddressPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"bad_persons_addresses\".* FROM \"bad_persons_addresses\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, badPersonsAddressPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in BadPersonsAddressSlice")
	}

	*o = slice

	return nil
}

// BadPersonsAddressExists checks if the BadPersonsAddress row exists.
func BadPersonsAddressExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"bad_persons_addresses\" where \"id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}

	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if bad_persons_addresses exists")
	}

	return exists, nil
}
