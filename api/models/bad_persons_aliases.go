// Code generated by SQLBoiler 3.5.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/pkg/errors"
	"github.com/volatiletech/null"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// BadPersonsAlias is an object representing the database table.
type BadPersonsAlias struct {
	ID          string      `boil:"id" json:"id" toml:"id" yaml:"id"`
	FullName    null.String `boil:"full_name" json:"full_name,omitempty" toml:"full_name" yaml:"full_name,omitempty"`
	BadPersonID string      `boil:"bad_person_id" json:"bad_person_id" toml:"bad_person_id" yaml:"bad_person_id"`
	UpdatedAt   time.Time   `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	CreatedAt   time.Time   `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	NameVector  null.String `boil:"name_vector" json:"name_vector,omitempty" toml:"name_vector" yaml:"name_vector,omitempty"`

	R *badPersonsAliasR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L badPersonsAliasL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var BadPersonsAliasColumns = struct {
	ID          string
	FullName    string
	BadPersonID string
	UpdatedAt   string
	CreatedAt   string
	NameVector  string
}{
	ID:          "id",
	FullName:    "full_name",
	BadPersonID: "bad_person_id",
	UpdatedAt:   "updated_at",
	CreatedAt:   "created_at",
	NameVector:  "name_vector",
}

// Generated where

var BadPersonsAliasWhere = struct {
	ID          whereHelperstring
	FullName    whereHelpernull_String
	BadPersonID whereHelperstring
	UpdatedAt   whereHelpertime_Time
	CreatedAt   whereHelpertime_Time
	NameVector  whereHelpernull_String
}{
	ID:          whereHelperstring{field: "\"bad_persons_aliases\".\"id\""},
	FullName:    whereHelpernull_String{field: "\"bad_persons_aliases\".\"full_name\""},
	BadPersonID: whereHelperstring{field: "\"bad_persons_aliases\".\"bad_person_id\""},
	UpdatedAt:   whereHelpertime_Time{field: "\"bad_persons_aliases\".\"updated_at\""},
	CreatedAt:   whereHelpertime_Time{field: "\"bad_persons_aliases\".\"created_at\""},
	NameVector:  whereHelpernull_String{field: "\"bad_persons_aliases\".\"name_vector\""},
}

// BadPersonsAliasRels is where relationship names are stored.
var BadPersonsAliasRels = struct {
	BadPerson string
}{
	BadPerson: "BadPerson",
}

// badPersonsAliasR is where relationships are stored.
type badPersonsAliasR struct {
	BadPerson *BadPerson
}

// NewStruct creates a new relationship struct
func (*badPersonsAliasR) NewStruct() *badPersonsAliasR {
	return &badPersonsAliasR{}
}

// badPersonsAliasL is where Load methods for each relationship are stored.
type badPersonsAliasL struct{}

var (
	badPersonsAliasAllColumns            = []string{"id", "full_name", "bad_person_id", "updated_at", "created_at", "name_vector"}
	badPersonsAliasColumnsWithoutDefault = []string{"full_name", "bad_person_id", "name_vector"}
	badPersonsAliasColumnsWithDefault    = []string{"id", "updated_at", "created_at"}
	badPersonsAliasPrimaryKeyColumns     = []string{"id"}
)

type (
	// BadPersonsAliasSlice is an alias for a slice of pointers to BadPersonsAlias.
	// This should generally be used opposed to []BadPersonsAlias.
	BadPersonsAliasSlice []*BadPersonsAlias

	badPersonsAliasQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	badPersonsAliasType                 = reflect.TypeOf(&BadPersonsAlias{})
	badPersonsAliasMapping              = queries.MakeStructMapping(badPersonsAliasType)
	badPersonsAliasPrimaryKeyMapping, _ = queries.BindMapping(badPersonsAliasType, badPersonsAliasMapping, badPersonsAliasPrimaryKeyColumns)
	badPersonsAliasInsertCacheMut       sync.RWMutex
	badPersonsAliasInsertCache          = make(map[string]insertCache)
	badPersonsAliasUpdateCacheMut       sync.RWMutex
	badPersonsAliasUpdateCache          = make(map[string]updateCache)
	badPersonsAliasUpsertCacheMut       sync.RWMutex
	badPersonsAliasUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single badPersonsAlias record from the query.
func (q badPersonsAliasQuery) One(ctx context.Context, exec boil.ContextExecutor) (*BadPersonsAlias, error) {
	o := &BadPersonsAlias{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for bad_persons_aliases")
	}

	return o, nil
}

// All returns all BadPersonsAlias records from the query.
func (q badPersonsAliasQuery) All(ctx context.Context, exec boil.ContextExecutor) (BadPersonsAliasSlice, error) {
	var o []*BadPersonsAlias

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to BadPersonsAlias slice")
	}

	return o, nil
}

// Count returns the count of all BadPersonsAlias records in the query.
func (q badPersonsAliasQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count bad_persons_aliases rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q badPersonsAliasQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if bad_persons_aliases exists")
	}

	return count > 0, nil
}

// BadPerson pointed to by the foreign key.
func (o *BadPersonsAlias) BadPerson(mods ...qm.QueryMod) badPersonQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.BadPersonID),
	}

	queryMods = append(queryMods, mods...)

	query := BadPersons(queryMods...)
	queries.SetFrom(query.Query, "\"bad_persons\"")

	return query
}

// LoadBadPerson allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (badPersonsAliasL) LoadBadPerson(ctx context.Context, e boil.ContextExecutor, singular bool, maybeBadPersonsAlias interface{}, mods queries.Applicator) error {
	var slice []*BadPersonsAlias
	var object *BadPersonsAlias

	if singular {
		object = maybeBadPersonsAlias.(*BadPersonsAlias)
	} else {
		slice = *maybeBadPersonsAlias.(*[]*BadPersonsAlias)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &badPersonsAliasR{}
		}
		args = append(args, object.BadPersonID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &badPersonsAliasR{}
			}

			for _, a := range args {
				if a == obj.BadPersonID {
					continue Outer
				}
			}

			args = append(args, obj.BadPersonID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(qm.From(`bad_persons`), qm.WhereIn(`bad_persons.id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load BadPerson")
	}

	var resultSlice []*BadPerson
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice BadPerson")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for bad_persons")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for bad_persons")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.BadPerson = foreign
		if foreign.R == nil {
			foreign.R = &badPersonR{}
		}
		foreign.R.BadPersonsAliases = append(foreign.R.BadPersonsAliases, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.BadPersonID == foreign.ID {
				local.R.BadPerson = foreign
				if foreign.R == nil {
					foreign.R = &badPersonR{}
				}
				foreign.R.BadPersonsAliases = append(foreign.R.BadPersonsAliases, local)
				break
			}
		}
	}

	return nil
}

// SetBadPerson of the badPersonsAlias to the related item.
// Sets o.R.BadPerson to related.
// Adds o to related.R.BadPersonsAliases.
func (o *BadPersonsAlias) SetBadPerson(ctx context.Context, exec boil.ContextExecutor, insert bool, related *BadPerson) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"bad_persons_aliases\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"bad_person_id"}),
		strmangle.WhereClause("\"", "\"", 2, badPersonsAliasPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.BadPersonID = related.ID
	if o.R == nil {
		o.R = &badPersonsAliasR{
			BadPerson: related,
		}
	} else {
		o.R.BadPerson = related
	}

	if related.R == nil {
		related.R = &badPersonR{
			BadPersonsAliases: BadPersonsAliasSlice{o},
		}
	} else {
		related.R.BadPersonsAliases = append(related.R.BadPersonsAliases, o)
	}

	return nil
}

// BadPersonsAliases retrieves all the records using an executor.
func BadPersonsAliases(mods ...qm.QueryMod) badPersonsAliasQuery {
	mods = append(mods, qm.From("\"bad_persons_aliases\""))
	return badPersonsAliasQuery{NewQuery(mods...)}
}

// FindBadPersonsAlias retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindBadPersonsAlias(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*BadPersonsAlias, error) {
	badPersonsAliasObj := &BadPersonsAlias{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"bad_persons_aliases\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, badPersonsAliasObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from bad_persons_aliases")
	}

	return badPersonsAliasObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *BadPersonsAlias) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no bad_persons_aliases provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	nzDefaults := queries.NonZeroDefaultSet(badPersonsAliasColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	badPersonsAliasInsertCacheMut.RLock()
	cache, cached := badPersonsAliasInsertCache[key]
	badPersonsAliasInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			badPersonsAliasAllColumns,
			badPersonsAliasColumnsWithDefault,
			badPersonsAliasColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(badPersonsAliasType, badPersonsAliasMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(badPersonsAliasType, badPersonsAliasMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"bad_persons_aliases\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"bad_persons_aliases\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into bad_persons_aliases")
	}

	if !cached {
		badPersonsAliasInsertCacheMut.Lock()
		badPersonsAliasInsertCache[key] = cache
		badPersonsAliasInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the BadPersonsAlias.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *BadPersonsAlias) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	key := makeCacheKey(columns, nil)
	badPersonsAliasUpdateCacheMut.RLock()
	cache, cached := badPersonsAliasUpdateCache[key]
	badPersonsAliasUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			badPersonsAliasAllColumns,
			badPersonsAliasPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update bad_persons_aliases, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"bad_persons_aliases\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, badPersonsAliasPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(badPersonsAliasType, badPersonsAliasMapping, append(wl, badPersonsAliasPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}

	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update bad_persons_aliases row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for bad_persons_aliases")
	}

	if !cached {
		badPersonsAliasUpdateCacheMut.Lock()
		badPersonsAliasUpdateCache[key] = cache
		badPersonsAliasUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q badPersonsAliasQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for bad_persons_aliases")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for bad_persons_aliases")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o BadPersonsAliasSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), badPersonsAliasPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"bad_persons_aliases\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, badPersonsAliasPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in badPersonsAlias slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all badPersonsAlias")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *BadPersonsAlias) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no bad_persons_aliases provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	nzDefaults := queries.NonZeroDefaultSet(badPersonsAliasColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	badPersonsAliasUpsertCacheMut.RLock()
	cache, cached := badPersonsAliasUpsertCache[key]
	badPersonsAliasUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			badPersonsAliasAllColumns,
			badPersonsAliasColumnsWithDefault,
			badPersonsAliasColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			badPersonsAliasAllColumns,
			badPersonsAliasPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert bad_persons_aliases, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(badPersonsAliasPrimaryKeyColumns))
			copy(conflict, badPersonsAliasPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"bad_persons_aliases\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(badPersonsAliasType, badPersonsAliasMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(badPersonsAliasType, badPersonsAliasMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert bad_persons_aliases")
	}

	if !cached {
		badPersonsAliasUpsertCacheMut.Lock()
		badPersonsAliasUpsertCache[key] = cache
		badPersonsAliasUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single BadPersonsAlias record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *BadPersonsAlias) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no BadPersonsAlias provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), badPersonsAliasPrimaryKeyMapping)
	sql := "DELETE FROM \"bad_persons_aliases\" WHERE \"id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from bad_persons_aliases")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for bad_persons_aliases")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q badPersonsAliasQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no badPersonsAliasQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from bad_persons_aliases")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for bad_persons_aliases")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o BadPersonsAliasSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), badPersonsAliasPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"bad_persons_aliases\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, badPersonsAliasPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}

	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from badPersonsAlias slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for bad_persons_aliases")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *BadPersonsAlias) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindBadPersonsAlias(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *BadPersonsAliasSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := BadPersonsAliasSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), badPersonsAliasPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"bad_persons_aliases\".* FROM \"bad_persons_aliases\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, badPersonsAliasPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in BadPersonsAliasSlice")
	}

	*o = slice

	return nil
}

// BadPersonsAliasExists checks if the BadPersonsAlias row exists.
func BadPersonsAliasExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"bad_persons_aliases\" where \"id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}

	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if bad_persons_aliases exists")
	}

	return exists, nil
}
